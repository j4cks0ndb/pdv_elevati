import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:pdv_elevati/app/modules/venda/venda_page.dart';

main() {
  testWidgets('VendaPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(VendaPage(title: 'Venda')));
    //  final titleFinder = find.text('Venda');
    //  expect(titleFinder, findsOneWidget);
  });
}
