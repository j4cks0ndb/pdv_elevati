import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:pdv_elevati/app/modules/venda/pages/lista_venda/lista_venda_page.dart';

main() {
  testWidgets('ListaVendaPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ListaVendaPage(title: 'ListaVenda')));
    //  final titleFinder = find.text('ListaVenda');
    //  expect(titleFinder, findsOneWidget);
  });
}
