import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:pdv_elevati/app/modules/venda/pages/lista_venda/lista_venda_controller.dart';
import 'package:pdv_elevati/app/modules/venda/venda_module.dart';

void main() {
  initModule(VendaModule());
  // ListaVendaController listavenda;
  //
  setUp(() {
    //     listavenda = VendaModule.to.get<ListaVendaController>();
  });

  group('ListaVendaController Test', () {
    //   test("First Test", () {
    //     expect(listavenda, isInstanceOf<ListaVendaController>());
    //   });

    //   test("Set Value", () {
    //     expect(listavenda.value, equals(0));
    //     listavenda.increment();
    //     expect(listavenda.value, equals(1));
    //   });
  });
}
