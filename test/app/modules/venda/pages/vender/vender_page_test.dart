import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:pdv_elevati/app/modules/venda/pages/vender/vender_page.dart';

main() {
  testWidgets('VenderPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(VenderPage(title: 'Vender')));
    //  final titleFinder = find.text('Vender');
    //  expect(titleFinder, findsOneWidget);
  });
}
