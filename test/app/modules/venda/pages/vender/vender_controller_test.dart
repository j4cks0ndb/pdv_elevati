import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:pdv_elevati/app/modules/venda/pages/vender/vender_controller.dart';
import 'package:pdv_elevati/app/modules/venda/venda_module.dart';

void main() {
  initModule(VendaModule());
  // VenderController vender;
  //
  setUp(() {
    //     vender = VendaModule.to.get<VenderController>();
  });

  group('VenderController Test', () {
    //   test("First Test", () {
    //     expect(vender, isInstanceOf<VenderController>());
    //   });

    //   test("Set Value", () {
    //     expect(vender.value, equals(0));
    //     vender.increment();
    //     expect(vender.value, equals(1));
    //   });
  });
}
