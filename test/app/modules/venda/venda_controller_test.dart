import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:pdv_elevati/app/modules/venda/venda_controller.dart';
import 'package:pdv_elevati/app/modules/venda/venda_module.dart';

void main() {
  initModule(VendaModule());
  // VendaController venda;
  //
  setUp(() {
    //     venda = VendaModule.to.get<VendaController>();
  });

  group('VendaController Test', () {
    //   test("First Test", () {
    //     expect(venda, isInstanceOf<VendaController>());
    //   });

    //   test("Set Value", () {
    //     expect(venda.value, equals(0));
    //     venda.increment();
    //     expect(venda.value, equals(1));
    //   });
  });
}
